
#include <cpp_contractgen>
#include <stdint.h>

#define MY_DOES_IT_WORK_MACRO(X) true

namespace TestNamespace {
define_contract MyComb {
    bool setDelaySamples(uint32_t d);
    float readTail(uint32_t i) const;
    void writeTail(uint32_t i, float s);
    void advance();
};
}
